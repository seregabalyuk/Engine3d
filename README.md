Проект на работает пока только на линуксе.

Нужно установить SDL библиотеку:
```bash
sudo apt-get install libsdl2-dev
```

Для компиляции проекта, напишите `make`

Для запуска тестов, напишите `make test`

