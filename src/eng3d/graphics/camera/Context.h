#pragma once

#include "../../../geom/Vector.h"

namespace eng3d::camera {
  struct Context {

  };

} // namespace eng3d

namespace eng3d::camera {
  struct PreparedNormal {
    using Vector = geom::Vector<Float, 3>;
    
    PreparedNormal() {

    }
  };
} // namespace eng3d

